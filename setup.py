from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is repo to combine the deep learning work on collapsing roofs in Baltimore City.',
    author='scottpitz',
    license='MIT',
)
